# Compute the log-odds

Compute the log-odds

## Usage

``` r
logit(x)
```

## Arguments

- x:

  scalar or vector in (0,1) for which to compute the (componentwise)
  log-odds

## Value

A scalar or vector of log-odds
